package com.bj58.argo;

import static org.mockito.Mockito.mock;

import javax.servlet.ServletException;

import org.testng.annotations.Test;


public class ArgoFilterTest {
	
	@Test
	public void testInit() throws ServletException {
//		FilterConfig filterConfig = mock(FilterConfig.class);
//
////		GroupConventionAnnotation groupConventionAnnotation = mock(GroupConventionAnnotation.class);
////		when(groupConventionAnnotation.groupConfigFolder()).thenReturn("/opt/argo");
////		when(groupConventionAnnotation.projectConfigFolder()).thenReturn("{groupConfigFolder}/{projectId}");
////		when(groupConventionAnnotation.projectLogFolder()).thenReturn("{groupConfigFolder}/logs/{projectId}");
////		when(groupConventionAnnotation.groupPackagesPrefix()).thenReturn("com.bj58.argo");
////		when(groupConventionAnnotation.projectConventionClass()).thenReturn("com.bj58.argo.MyProjectConvention");
////		when(groupConventionAnnotation.controllerPattern()).thenReturn("com\\.bj58\\..*\\.controllers\\..*Controller");
////		when(groupConventionAnnotation.viewsFolder()).thenReturn("/WEB-INF/classes/views/");
////		when(groupConventionAnnotation.staticResourcesFolder()).thenReturn("/WEB-INF/classes/static/");
//
//		ServletContext servletContext = mock(ServletContext.class);
//		when(filterConfig.getServletContext()).thenReturn(servletContext);
//		when(servletContext.getContextPath()).thenReturn("/oss/web/post.58.com");
//
//
//		ArgoFilter argoFilter = new ArgoFilter();
//		argoFilter.init(filterConfig);

	}
	

	

}
